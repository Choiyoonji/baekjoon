#include<iostream>
#include<stack>
using namespace std;

stack<int> s;
int n, ans, gmin, lmin, lmax;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);


    cin >> n; 
    gmin = 1000000001;
    lmin = 1000000001;
    
    for(int i = 0; i < n; i++){
        int k;
        cin >> k;

        

        if(gmin > k) gmin = k;

        if()
    }
}